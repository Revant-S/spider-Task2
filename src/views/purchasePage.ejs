<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Purchase <%= book.title %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/public/cssFiles/purchasePage.css">
</head>

<body>
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-6">
                <img src="<%= book.imageLink %>" alt="<%= book.title %>" class="img-fluid book-image">
            </div>
            <div class="col-md-6">
                <h1 class="book-title">
                    <%= book.title %>
                </h1>
                <p class="book-authors">By <%= book.authors.join(', ') %></p>
                <p class="book-publisher"><%= book.publisher %></p>
                <div class="book-genre">
                    <% book.genere.forEach(genre => { %>
                        <span class="badge bg-secondary me-1"><%= genre %></span>
                    <% }) %>
                </div>
                <p class="book-description"><%= book.description %></p>
                <div class="book-stats">
                    <span class="me-3"><i class="bi bi-heart-fill"></i> <%= book.likes %> likes</span>
                    <span><i class="bi bi-chat-fill"></i> <%= book.reviews.length %> reviews</span>
                </div>
                <div class="book-price mt-3">
                    <h3>Price: <%= book.cost.amount %> <%= book.cost.currencyCode %></h3>
                </div>
                <button class="btn btn-primary btn-lg mt-3 purchase-btn" id="purchaseNowBtn" data-BookId="<%= book._id %>">Purchase Now</button>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/public/dist/purchaseScript.bundle.js"></script>
</body>
</html>